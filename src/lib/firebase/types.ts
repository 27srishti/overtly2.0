export interface client {
    name: string;
    industry: string;
    domain: string;
    demographics: string;
    createdAt: number;
}

export interface Project {
    id: number;
    project: number;
    name: string;
    price: number;
}